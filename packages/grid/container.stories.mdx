import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Template } from './container.stories';
import { Container, GridConfigProvider } from './src';

export const gridConfig = {
  breakpoints: { xs: 15, sm: 20, md: 25, lg: 30 },
  gutter: '1rem',
};

<Meta
  title="Grid/Container"
  decorators={[
    (story) => (
      <GridConfigProvider value={gridConfig}>{story()}</GridConfigProvider>
    ),
  ]}
  component={Container}
/>

# Container

<Canvas withToolbar>
  <Story name="Container">{Template.bind({})}</Story>
</Canvas>

<ArgsTable of="." story="^" />

## Responsive Container

The `Container` responsively sets a `max-width` for each configured breakpoint equal to `calc(breakpoint - gutter)`.

## Fluid Container

When used with the `fluid` prop, `max-width` is always `100%`.
