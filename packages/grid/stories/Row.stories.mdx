import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Template } from './Row.stories';
import { Row, GridConfigProvider } from '../src';
import { Box } from './Box';

export const gridConfig = {
  breakpoints: {  
    xs: 30,
    sm: 48,
    md: 64,
    lg: 75,
  },
  gutter: '1rem',
};

<Meta
  title="Grid/Row"
  decorators={[
    (story) => (
      <GridConfigProvider value={gridConfig}>{story()}</GridConfigProvider>
    ),
  ]}
  component={Row}
/>

# Row

<Canvas withToolbar>
  <Story name="Row">{Template.bind({})}</Story>
</Canvas>

<ArgsTable of="." story="^" />
